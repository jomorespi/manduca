---
import FooterMenu from "./FooterMenu.astro";

const { txt, links, class: className } = Astro.props;
---

<footer class:list={['text-center text-sm text-muted', className]}>
    <p>{ txt }</p>
    <FooterMenu links={ links } />
</footer>